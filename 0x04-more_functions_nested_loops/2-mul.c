#include "main.h"
#include <stdio.h>
/**
 * mul - fuction that multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: mul.
 */
int mul(int a, int b)
{
	int x;

	x = a + b;
	return (x);
}
